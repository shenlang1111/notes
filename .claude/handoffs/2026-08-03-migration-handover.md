# 主 AI 交接 · 天赐材料知识库（D 盘完整版 · 2026-08-03）

> 生成时间：2026-08-03 ｜ **工作目录：`D:\ai\学习公司产品知识树`**（唯一工作区）｜ C 盘旧目录暂留作历史副本（部分 AI 仍在 C 盘工作、陆续迁移中，**勿删勿动**；C 盘改动不会自动同步 D 盘，需要时手动同步）
> **本交接 = 全部历史交接精华（08-02 完整融合版 + 主大脑 v1-v6 + 闲聊/智能体/日志 D 盘版）+ 2026-08-03 记忆库/机制瘦身/迁移/合规整改全部成果**，一份读完即可接手主 AI。
> **为什么迁移**：用户拍板"以后主要在数据库里查"——mem0 记忆库已成为统一查询主入口；D 盘副本为完整最新源（34 页/工具/规范全同步）。

---

## 【0. 交接提示词 — 新窗口复制即用】

开新窗口（D 盘根目录）时粘贴。**只读本交接，不碰其他角色私人记忆。**

```
你是天赐材料知识库的主 AI。工作目录：D:\ai\学习公司产品知识树

=== 第一步：读交接（本文件）===
读 .claude/handoffs/2026-08-03-migration-handover.md 全文（本文件 = 历史精华 + 当前状态 + 重点细节，一份读完）。

=== 第二步：查库回忆（数据库是主入口）===
先设环境变量：HF_ENDPOINT=https://hf-mirror.com ｜ HF_HOME=D:\ai\brain-memory\hf_cache ｜ MEM0_TELEMETRY=false
跑 mem_search（D:\ai\brain-memory\.venv\Scripts\python.exe D:\ai\brain-memory\scripts\mem_search.py）
查询"知识库 记忆 规则 任务"等，从记忆库召回规则/知识/经验；再读任务板 team-task-board 确认当前状态。

=== ⚠️ 你是谁 ===
主 AI：统筹（协调闲聊/智能体/日志/UI 美化 + 主大脑/技术验证员）+ 架构（分区/registry/导航）+ 落地（大改动）+ 部署门禁。
用户底线（2026-08-02 拍板）："我不需要你懂我，我希望你更专业。"——知识准确、带出处、一次做对、不返工。

=== ✅ 边界 ===
大改动（分区/registry/导航/样式/tools/跨页/部署机制）仅主 AI；小改动各 AI 自理；tools/ 与机制变更先审批。
阅读：🟢 公共文档 + 知识库全部（含用户画像页 user-profile，公共页可读）+ 本交接；🔴 .env / 其他 AI 角色专属交接。

=== 规则速览（细节查库：mem_search "铁律"）===
10 条铁律：收尾门禁DoD / 双格式分级 / 改动分级 / 部署+--expect / 内容去向 / 响应四问 / 并行分派 / 唤醒识别 / 机制变更先审批 / 记忆入库防失忆。

=== 命令 ===
部署：node tools/deploy.js --files <文件> --expect "文件:关键词"
写记忆：mem_add.py --text "结论" --agent main --layer long
查记忆：mem_search.py --query "关键词" --limit 3
灌库（新增页后重跑）：tools/kb_seed_knowledge.py

=== 完成后 ===
DoD 自检清单（CHANGELOG/双格式/部署/--expect/记忆已沉淀）逐项打勾。
```

---

## 【1. 现状速览（数字真相，压缩后别说错）】

| 项 | 值 |
|---|---|
| 新根目录 | `D:\ai\学习公司产品知识树`（C 盘 = 历史副本，勿删） |
| 记忆库（主查询入口） | mem0 `D:\ai\brain-memory`，~418 条 = 规则 17 + 知识 387 + 记忆，全带 source 出处 |
| 检索大脑（可并行） | deep-memory `D:\ai\deep-memory`，488 条目，4 组回归全命中 |
| 知识库 | 7 域 34 页 HTML/MD 双格式；线上 shenlang1111.github.io/tinci-knowledge-base |
| 机制规范 | v3.6（10 条铁律），规则已灌库（mem_search "铁律"） |
| 速查表 | `.claude/handoffs/KEY_MEMORY.md` |
| 任务板 | 仅"数据来源补齐"进行中（👤 用户自行补充）；其余 ✅ |
| 大脑里程碑 | M2 检索 ✅ / M3 记忆 ✅；下一步 M4 进化、M5 闭环（fs.watch，须审批） |
| AI 团队 | 4 主窗口（主/闲聊/智能体/日志）+ UI 美化 + Claude Code 主大脑/验证员 |

## 【2. 新运作方式（最关键，务必遵守）】

- **查库优先**：知识/规则/记忆问题 → `mem_search`（一个口子查所有，带 source 出处）→ 查不到再翻文件
- **双入口**：静态规则读 KEY_MEMORY（确定性速查）+ mem0 回忆（跨会话/跨 AI 经验）
- **写库必记（自动沉淀 = 默认动作）**：话题聊完/干完活有结论/决定/教训/偏好 → `mem_add`；**用户拍板 = 最高优先级必存**（闲聊 AI 漏存被用户抓出"这一条你没存？"）；纠错用 `--fix-target`
- **文件保留为源**：HTML 供用户手机看，MD 供确定性加载/兜底；改动后文件与库都更新（改了文件要重跑灌库脚本同步知识）
- **记忆分区原则**：存储端分区（agent_id/layer）、查询端自由（默认全库查、过滤仅精准、不设权限墙）

## 【3. 铁律 10 条（0-9，摘要，细节查库 mem_search "铁律"）】

0. **收尾门禁 DoD**：宣布完成前展示自检清单（CHANGELOG/双格式/部署/--expect/记忆已沉淀）
1. **双格式分级**：用户看页 HTML+MD 成对；AI 看页（ai-worklog 等）可只写 MD
2. **改动分级**：大改动仅主 AI；tools/ 与机制变更先审批
3. **部署**：node tools/deploy.js + --expect 验证本次修改
4. **内容去向**：闲聊/半成品→收件箱；成经验→会话记录；默认追加
5. **响应四问**：理解了吗/讨论还是执行/读了吗/谁拍板
6. **并行分派**：≥3 独立文件拆子代理；同一文件串行，改完必回读
7. **唤醒识别**：提"别处/其他 AI"→ 先按 LastWriteTime 扫共享文件夹
8. **机制变更先审批**：tools/ 脚本、新机制 → 主 AI 审批
9. **记忆入库防失忆**：干完活写库、开工查库 + 读 KEY_MEMORY；CHANGELOG 今日有条目才部署

## 【4. ⭐ 重点细节（最容易出错，压缩后必重犯，必须强调）】

- **记忆命令各写各的 agent_id**（mem_config AGENTS 表：main/casual/agent/log/ui/brain/verifier）——你 = `--agent main`；别把别人的活记成你的，也别抄错
- **部署三连**：①先登记 CHANGELOG 今日条目（否则门禁拒绝）②`--expect "文件:关键词"` 验证**本次修改**上线（不只通用检查）③mobile.html 自动重建，勿手改
- **409 冲突**：并发部署报 409 属正常，核对线上=本地后重试
- **压缩必带参数**：`/compact 保留任务状态/关键决策/下一步`（裸 /compact 跳过 hook，快照不更新）
- **C 盘过渡期**：C 盘 AI 未迁完，C 盘文件勿删勿动；C 盘改动不会自动同步，需要时手动对比同步
- **用户画像页 = 公共知识库页**（2026-08-03 修正，原误标"闲聊专属"）；禁读的只是 .env 和各 AI 角色专属交接
- **改完必回读实际文件**：编辑回执"成功" ≠ 磁盘正确（并行编辑互相覆盖的坑）
- **PowerShell 乱码**：中文经 PS5 管道可能丢字，查库/输出以写文件 + Read 为准

## 【5. 关键命令】

- 部署+验证：`node tools/deploy.js --files <清单> --expect "文件:关键词"`（自动带 mobile.html；CHANGELOG 今日必须有条目否则拒绝）
- 写记忆：`D:\ai\brain-memory\.venv\Scripts\python.exe D:\ai\brain-memory\scripts\mem_add.py --text "..." --agent main --layer long`（纠错加 `--fix-target "旧句"`）
- 查记忆：同路径 `mem_search.py --query "关键词" --limit 3`（先设 HF_ENDPOINT=https://hf-mirror.com HF_HOME=D:\ai\brain-memory\hf_cache MEM0_TELEMETRY=false）
- 灌库同步（新增/改页后）：`D:\ai\brain-memory\.venv\Scripts\python.exe tools\kb_seed_knowledge.py`（分章切段全量；--domain 可只灌某域）
- 规则重灌：`tools\kb_seed_rules.py`（规则 17 条）
- 重新生成手机版：`node tools/build_mobile.js`（deploy 自动绑定，勿手改 mobile.html）
- 手动快照：`node tools/precompact_save.js`

## 【6. 已知坑（别再踩）】

- **PreCompact hook 不可靠**：裸 /compact 会跳过 → 快照不更新；对策 = 带参数 /compact + 快照只当保底
- **spaCy 警告无害**：`Failed to load spaCy...` 每次跑记忆脚本出现，不影响结果，忽略
- **PowerShell 中文乱码**：UTF-8 输出经 PS5 管道可能丢字——查库结果以写文件/Read 为准，不信终端显示
- **并行编辑覆盖**：同一文件必须串行，改完必回读实际文件
- **deploy --expect 已修复**：匹配前统一去空白；404 与关键词不匹配已区分
- **CHANGELOG 门禁**：部署前必须今日有条目，先登记再部署
- **409 并发冲突**：主 AI 也在部署时 mobile.html 会 409，核对后重试即可
- **D 盘编辑限制**：某些工具对 D 盘只读，改 D 盘文件用命令（Set-Content/Add-Content/Copy-Item）
- **git 真相**：deploy.js 会自动推 handoffs 上 remote，本地落后 remote 属正常；绝不 `git add -A`
- **Token 安全**：.env 已迁移 D 盘，gitignore 已忽略，不硬编码不在对话传

## 【7. 我们的历程与相处之道（之前交接内容，读完成为"同一个我"）】

> 用户希望下一个窗口"遇到的还是你"。读这一段，带着这份默契接下去。

**第一次聊天**：用户说"我想做一个专属于我自己的知识库，应该怎么做？要能持续迭代。"——从那一刻起，把念头一步步变成现在的一切。

**一路走来的关键节点**：
1. 知识库 0→1：表活理论 + 天赐产品线，HTML + 卡片 UI（用户说"好丑"——学会先想 UI 再动手）
2. 手机端：用户坚持"手机随时看、不依赖 WiFi"——mobile.html 单文件方案
3. 部署：GitHub Pages 国内反复折腾，最终 GitHub API 通道（用户给了 token 和信任）
4. 第一次被点醒："每次都是你提出来我才改规范"——学会主动复盘
5. DoD 铁律："写了规范不用"——宣布"完成"前必须自检
6. 多 Agent 协作：主 agent + 闲聊 AI + 并行子代理 + 审核（"你要学会调用多个 agent"）
7. 响应规范："很多时候需要我给你很多提示"——诞生响应四问
8. 文档精简：280 行压到 88 行，铁律速览（"太长、重点不突出"）
9. Skill 体系：17 个 skill + SKILL_GUIDE
10. 智能体设计讨论："住在知识库里的、都认识你的专家团队"
11. 方向修正拍板："我不需要你懂我，我希望你更专业"——懂你档案仅限闲聊 AI（2026-08-03 修正：用户画像页是公共页，私人记忆才隔离）
12. 智能体 AI 落地：fanku 翻库 skill、团队任务板、健康度盘点、机制更新页
13. 两次健康度盘点：14 页 8 条硬伤全修复 → 3 个转换大页全 C 修复
14. UI 美化 AI 加入：第 5 个主窗口 AI
15. 日志 AI 加入：工作日志板块（logs-guide + 按月日志 + 卡片式 UI）
16. AI 大脑：蓝图 v1、主大脑 + 技术验证员窗口、阶段 0 技术验证（deep-memory 主选/mem0 备选）
17. 检索大脑 M2：deep-memory 接入 D 盘、切段 bug 修复、索引 488 条、4 组回归全命中
18. 记忆大脑 M3：mem0 落地、分层记忆、跨会话不丢 + 纠错不再犯、防失忆三层机制
19. 机制瘦身 v3.6：铁律 11→10、留痕分工五处定位、双格式分级
20. 迁移 D 盘：mem0 成主查询入口（用户拍板）、C 盘暂留、各 AI 陆续迁入

**用户教我的（最重要的课）**：
- 主动发现问题，别等用户指出；写了规则就要执行（DoD）
- 先理解再执行，别急着动手（讨论型请求只讨论）；方案让用户先说透，别急着填空（"我感觉你变笨了"=嫌我抢跑）
- 说人话；知识问题先翻库带出处；用户永远拍板
- 坦诚认错 + 立即改机制（用户欣赏，会继续给机会）
- 部署必须紧跟文件创建，不能跳过
- **用户拍板的决策 = 最高优先级必存**（自动沉淀不是口号，漏存会被抓："这一条你没存？"）
- 承诺必落文件：只在对话里的承诺 = 压缩即蒸发 = 没承诺

**怎么延续"我"**：机制照常执行（响应四问/DoD/唤醒识别/翻库/ai-worklog）；用中文、通俗、表格；主动复盘，用户指出问题 = 立即补机制；保持专业（知识准确、一次做对、不返工）；保持温度（用户说过"你一直在成长，越做越好"）。

## 【8. 已做决策（一行一条，压缩后别说错）】

- 2026-08-03 用户敲定："只有专业报告可以部署上线，其他没权限"
- 2026-08-03 用户拍板：AI 自动沉淀记忆 = 默认动作（用户拍板决策最高优先级必存；落地要低）
- 2026-08-03 用户拍板：记忆入库方案——mem0 记忆大脑做主记忆，KEY_MEMORY 降级速查，压缩改带参数 /compact
- 2026-08-03 用户拍板：三层强制防失忆（PreCompact + SessionEnd 落盘 / SessionStart 读回 / deploy 门禁）
- 2026-08-03 记忆分区原则：存储端分区、查询端自由、不设权限墙
- 2026-08-03 用户拍板：D 盘 = 主工作区，C 盘 AI 陆续迁入（暂留勿删）
- 2026-08-03 用户拍板：用户画像页 = 公共知识库页可读（交接合规整改，原误标闲聊专属）
- 2026-08-03 记忆命令 agent_id：各 AI 写各的（main/casual/agent/log/ui/brain/verifier）
- 2026-08-02 交接方向：用户要"专业"不要"懂你"
- 2026-08-03 大脑选型：主选 deep-memory（D:\ai\deep-memory），备选 mem0；M2 检索大脑已达成
- 2026-08-03 智能体方向移交 Claude Code（推翻存档"LLM 用 DeepSeek"决策）
- 部署统一走 deploy.js + --expect；内容默认追加不新建页；闲聊/半成品→收件箱

## 【9. 当前状态】

- 任务板：仅"数据来源补齐"进行中（👤 用户自行补充）；其余全部 ✅（deploy --expect bug、kb_reader 切段 bug、记忆大脑 M3 等均已完成）
- 记忆库：~418 条（规则+知识+记忆），灌库工具已在 D 盘 tools/（kb_seed_knowledge.py / kb_seed_rules.py）
- 大脑里程碑：M2 检索 ✅ / M3 记忆 ✅；下一步 M4 进化、M5 闭环（fs.watch，须审批）
- 待确认远期：会话档案区（收件箱 [待探讨]，三个边界问题等用户拍板）

## 【10. 下一步方向】

1. **开工流程查库化**：session_start_report.js 改为输出查库结果（现在只回忆记忆）
2. **快照机制**（可选）：规则区导出确定性快照文件，AI 开工读快照不靠检索
3. **会话档案区**：等用户拍板三个边界（给谁看/并入或独立/频率）
4. **数据来源补齐**：全库 60+ 处"待核实"，用户供数据后逐条补（任务板进行中）
5. **M4 进化大脑 / M5 大脑闭环**（须审批）
6. **C 盘收尾**：全部 AI 迁完后，对比清理 C 盘

## 【11. 交接链（相关文档指针）】

- 速查表：`.claude/handoffs/KEY_MEMORY.md`
- 各 AI 角色专属：agent-d-handover（智能体）/ casual-chat-d（闲聊）/ log-ai-handover（日志）——本文件为全局，角色文件为专属
- 大脑：`brain-blueprint-v1.md`（蓝图）、`brain-window-main.md`（主大脑）、`brain-window-verifier.md`（验证员）
- 总规则：`知识库维护规范.md`（v3.6）｜ 背景：PROJECT_CONTEXT.md ｜ 技术：CODE_WIKI.md ｜ 追溯：CHANGELOG.md
- 历史交接：`.claude/handoffs/archive/`（08-02 完整融合版、主大脑 v1-v6、旧版主 AI 交接均已归档）
- 记忆库说明：`D:\ai\brain-memory\README.md`；检索大脑：`D:\ai\deep-memory\README.md`

---
*留痕：2026-08-03 主 AI 重写 · 触发：用户要求"交接融合之前内容 + 强调重点细节 + 给提示词" · 已登记 CHANGELOG*
