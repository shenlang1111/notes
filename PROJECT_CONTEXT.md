# 项目上下文 · 天赐材料日化知识库

> 给下一个 AI 会话的交接文档。读完这个文件就能理解项目背景、用户需求、当前状态。**协作规则详见《知识库维护规范》**（v3.3，规则总纲以它为准）。
>
> ⚠️ **接手前必做**：读最新会话交接 → `.claude/handoffs/` 下按日期最新的文件；读智能体设计存档 → `knowledge-base/domains/思考收件箱/inbox-20260802-agent-design.html`。
>
> **文档关系**：总规则 →《知识库维护规范》（★★★ 必读）；技术细节 → CODE_WIKI.md（★ 推荐）；技能能力 → SKILL_GUIDE.md（★）；修改追溯 → CHANGELOG.md（☆ 按需）。本文档 = 项目背景交接（★★ 新会话必读）。

---

## 用户画像

- **身份**：天赐材料（Tinci Materials）日化销售
- **目标**：系统学习表面活性剂及相关日化原料知识，从"知道产品叫什么"到"理解产品为什么这样"，最终能向客户讲清楚技术原理
- **学习方式**：通过和 AI 对话讨论概念，自动整理到知识库网页；常在多个对话框并行协作（主 agent + 闲聊/内容 agent）
- **知识起点**：有基础化学概念（认识羟基、羧基等），但对表活深层原理不熟
- **偏好**：喜欢通俗类比（漏斗、乐高零件、小触角、水外套）、表格、销售实战视角；反感"文字墙"和流水账式记录；喜欢自由讨论，不喜欢被选项框住
- **痛点**：不希望每次更新都要说一大堆；要求内容沉淀为"可复用经验"而非流水账；反感 AI"急着执行、不懂就问清再动"

## 核心诉求

- 手机上随时可看（不依赖 WiFi，流量即可访问）
- AI 随时可调用其中的内容（内容同时存 HTML + Markdown）
- 持续增量迭代

---

## 项目结构（当前状态）

```
学习公司产品知识树/
├── knowledge-base/                  # ★ 知识库本体
│   ├── index.html                   # 桌面首页（7 大分类导航）
│   ├── mobile.html                  # 手机版单文件（build_mobile.js 自动生成，勿手改）
│   ├── registry.json                # 内容注册表（页面元数据索引）
│   ├── domains/                     # 内容页 HTML（按领域分文件夹，29 个专题页 + 收件箱/日志）
│   │   ├── 表面活性剂/              # 11 页（fundamentals/anionic/cationic/amphoteric/nonionic/properties/synthesis/applications/products/advanced/troubleshooting）
│   │   ├── 日化原料与配方/          # 1 页（formulation）
│   │   ├── 销售与市场/              # 4 页（market/sales + 天赐学习手册 tinci-surfactant-guide + 两性表活目录 tinci-amphoteric-summary）
│   │   ├── 会话记录/                # 3 页（session-20260801 与 AI 的交流经验、session-prompt 新会话开场话术、user-profile 用户画像）
│   │   ├── 思考收件箱/              # 6 页（inbox 想法缓冲区、inbox-20260802-agent-design 智能体设计存档、ai-worklog AI 工作记录、team-task-board 团队任务板、kb-health-checklist 健康度盘点、mechanism-updates 机制更新）
│   │   ├── 精细化工与扩展/          # 1 页（finechem-engineering 精细化工工艺学核心提炼）
│   │   └── 工作日志/                # 3 页（logs-guide 板块说明、logs-2026-07、logs-2026-08 按月的日志模板）
│   ├── markdown/                    # 内容页 MD 版（AI 检索用，与 HTML 成对维护）
│   └── _shared/
│       ├── css/style.css            # 共享样式（青绿系）
│       └── js/nav.js                # 共享导航脚本（自动渲染顶部导航）
├── tools/
│   ├── build_mobile.js              # 生成 mobile.html（合并首页+28 页）
│   └── deploy.js                    # 一键部署（校验→生成→上传→构建→验证）
├── index.html                       # 根入口（跳转到 mobile.html）
├── 知识库维护规范.md                 # ★ 协作总规则（v3.2，精简版：9 条铁律 + 分节）
├── SKILL_GUIDE.md                    # 技能使用指南：项目可用 skill 清单 + 调用方式（★ 推荐）
├── CHANGELOG.md                     # 变更日志（每次交流修改的追溯）
├── PROJECT_CONTEXT.md               # 本文档
├── CODE_WIKI.md                     # 技术维基（v3.1，承接技术细节/命令）
├── .env                             # 部署 Token（已被 .gitignore 忽略）
└── .gitignore
```

> 已清理：早期独立 HTML 包（surfactant-chapter3、tinci-*、content-workbench 等）、临时目录（.screenshots/.uploads/.trae-html-share-packages）、旧工具脚本均已在本地和 GitHub 仓库删除。

## 首页 7 大分类

| 分类 | 页面 |
|---|---|
| 基础理论 | 基础概念、物化性质、合成工艺、应用原理 |
| 四大表面活性剂 | 阴离子、阳离子、两性、非离子 |
| 实战应用 | 产品对照、配方设计、问题诊断、销售话术 |
| 扩展阅读 | 新型表活与检测、市场趋势、精细化工工艺学 |
| 产品手册 | 天赐学习手册、两性表活产品目录（销售培训用书） |
| 工作日志 | 日志说明、按月日志（logs-2026-07 / logs-2026-08） |
| AI 协作与日常 | 与 AI 的交流经验、新会话开场话术、用户画像、思考收件箱、AI 工作记录、机制更新、团队任务板、健康度盘点（日化之外的内容都归此类） |

## 双格式规则

- 每条内容同时维护：`domains/<域>/<页>.html`（网页展示）+ `markdown/<域>/<页>.md`（AI 检索）
- MD 头部含元信息：title / domain / tags / description / updated
- 新增/修改内容流程：写内容 → HTML → MD → registry.json → 重新生成 mobile.html → 部署

## 内容去向（重要）

- **思考收件箱**（想法缓冲区）：闲聊讨论、零散半成品、没想清楚的话题先放这里（按日期/话题分文件），别丢
- **会话记录**：整理成"可复用经验"后移入（默认追加已有页面，新建页需用户确认）
- 每次聊天结束都要记录、总结、精炼，并部署上线

## 导航机制（共享脚本）

- 顶部导航由 `knowledge-base/_shared/js/nav.js` 统一渲染（自动计算根路径 + 当前页高亮）
- **新增页面时只需在 nav.js 的链接清单加一项**，所有页面导航自动同步
- 禁止在页面里硬编码导航链接

## 部署机制

- 线上站点：https://shenlang1111.github.io/tinci-knowledge-base/（GitHub Pages）
- 部署统一走 `node tools/deploy.js`（不手动传文件）
  - 自动：双格式校验 → 重新生成 mobile.html → 串行上传（含 sha 冲突检测）→ 触发构建 → 轮询 → 服务器端验证
  - 验证本次修改内容：`--expect "文件路径:关键词"`
  - Token 从 `.env` 读取（GitHub API 通道，国内直连稳定；git push 依赖加速器不可靠）
- 部署后必须以**服务器端抓取验证**确认本次修改上线（见规范三-3）
- **大改动规则不豁免部署**：任何文件创建/修改后立即部署

## 协作工作流（简化）

1. 每次交流结束：总结提炼（可复用经验，非流水账）→ 更新规范（如需）→ 记 CHANGELOG → 更新网页并部署
2. 任务完成前必须过 **DoD**（规范第七节）：回复"完成"前展示自检清单
3. **并行分派**：≥3 个独立步骤/文件的任务默认拆子代理并行（各碰各的文件，同一文件串行）；关键产出配审核 agent
4. **响应四问**（规范第八节）：理解了吗 / 讨论还是执行 / 读了吗 / 谁拍板——减少用户反复提示
5. **主动复盘**：用户指出问题 = 我本该更早发现，立即补机制防重犯（规范第九节）
6. **AI 团队与协作留痕**：
   - 团队分工：主 AI（统筹落地）/ 闲聊 AI（交流陪伴）/ 智能体 AI（翻库与知识库自我建设）/ 日志 AI（工作日志与报告）；主 AI 手下还有审核/内容/检索子代理
   - 新机制广播：新增协作机制（页面/规则/skill/流程）后到 `mechanism-updates.html` 追加一条（谁建的+去哪读），新 AI 到岗先读它
   - 干完活到 `domains/思考收件箱/ai-worklog.html` 登记一行（日期 + 名字 + 做了什么 + 涉及位置），其他 AI 靠它知道谁干过什么
   - 多人协作任务在 `team-task-board.html` 登记/认领；知识库定期做 `kb-health-checklist.html` 健康度盘点
7. **翻库规则**（知识问题必先查库）：先翻库、再开口；带出处、不编造；翻不到、说没有（完整见 `.trae/skills/fanku/SKILL.md`）

## 用户偏好和沟通风格

1. 喜欢表格和可视化，不喜欢纯文字段落
2. 喜欢通俗类比；每个概念都要落到"怎么跟客户讲"
3. 要求准确性：产品分类、INCI 名称、公司数据必须核实；数据标注来源
4. 说中文；简洁交互：说一句模糊想法 → AI 理解、研究、更新
5. 内容要"可复用经验"（场景 → 做法 → 下次怎么用），不要流水账
6. 回复要精简，不要一次输出太多
7. 自由讨论优先：用户说"讨论/完善"时只讨论不执行，决定权在用户

## 重要产品信息（已核实）

- **TC-CAB**：椰油酰胺丙基甜菜碱，羧基甜菜碱
- **TC-LAB**：月桂酰胺丙基甜菜碱，C12 短碳链
- **TC-MAB**：咪唑啉型，Disodium Lauroamphodiacetate
- **TC-SHD**：磺基甜菜碱，Cocamidopropyl Hydroxysultaine
- **TC-CAO/TC-LAO**：氧化胺类，两性表活
- **GCK30P → AMIN GCK95U(P)**：椰油酰甘氨酸钾
- **CG30W → AMIN CG30**：椰油酰谷氨酸二钠
- **PQ-7 → POLYQUTA 550/550F**：聚季铵盐-7
- **销量数据**：2025 年日化材料销量突破 12 万吨，全球第三大两性表活生产商（市占率 10.6%）

---

> 文档版本：v3.3 | 最后更新：2026-08-03 | 协作规则以《知识库维护规范》v3.3 为准
