# 知识库维护规范

> 版本：v3.8（2026-08-03 规则修订体系：新增十·四——发现问题有闭环、不靠人盯；部署权限同 v3.7：大脑体系子 AI 仅专业报告可上线、主 AI 及各主窗口 AI 有部署权，见三-7/五-2）· 总规则（★★★ 必读）
> 文档关系：背景 → PROJECT_CONTEXT.md（★★）｜ 技术细节/命令 → CODE_WIKI.md（★）｜ 技能能力/调用 → SKILL_GUIDE.md（★）｜ 修改追溯 → CHANGELOG.md（☆）｜ 历史经验 → 知识库"与 AI 的交流经验"页

## 〇、铁律速览（先看这 10 条）

0. **收尾门禁（DoD）**（七）：宣布"完成"前必须展示自检清单（CHANGELOG/双格式/部署/--expect/记忆已沉淀）；回复无自检清单 = 未完成，用户可直接判定
1. **双格式分级**（二-2）：**用户看**的页保持 HTML+MD 双格式（知识内容页、会话经验、工作日志、机制更新、任务板）——**硬门禁，DoD 必过**；**AI 看**的页可只写 MD（ai-worklog、CHANGELOG 等治理记录），成对校验仅告警不阻断
2. **改动分级**（六-2）：小改动（单页/单 MD）各 agent 自理；大改动（分区、registry、导航、样式、tools/、跨页批量、部署机制）仅主 agent，其他人只建议不执行
3. **部署**（三）：一律 `node tools/deploy.js`；部署后必须 `--expect "文件:关键词"` 验证**本次修改**上线，不能只做通用检查；**主 AI 及各主窗口 AI 有部署权**（正式内容即部署）；**大脑体系子 AI（Claude Code 主大脑/技术验证员）只有专业报告可部署上线**，其余产出无部署权限（2026-08-03 用户拍板，分级见三-7）
4. **内容去向**（二-7/五-1）：闲聊/半成品 → 思考收件箱；整理成经验 → 会话记录；默认追加已有页，新建页有门槛（膨胀超 30 条或独立成体系）且先问用户
5. **响应四问**（八）：理解了吗 / 讨论还是执行 / 读了吗 / 谁拍板——每次回复前必答
6. **并行分派**（六-5）：≥3 个独立步骤/文件的任务默认拆子代理并行（各碰各的文件；同一文件必须串行编辑）
7. **唤醒识别**（六-6）：用户提"别处/其他 AI/最近修改"→ 先按 LastWriteTime 扫共享文件夹，不说"看不到"
8. **机制变更先审批**（六-7）：新建/修改 tools/ 脚本、新增机制、工具类变更 → 先报主 AI 审批，获批再执行（不自我授权）
9. **记忆入库防失忆**（十）：记忆放库里不靠脑——干完活必写库（mem_add）、开工/压缩后必查库（mem_search）+ 读 KEY_MEMORY 一页纸；部署前 deploy.js 强制 CHANGELOG 今日有条目，无则拒绝部署；改完必回读实际文件（并行编辑会互相覆盖）

## 一、项目定位

个人可持续迭代知识库：手机随时可看（不依赖 WiFi）、AI 随时可调用、持续增量迭代。

## 二、内容规范

1. **分区**：当前有内容域 = 表面活性剂 / 日化原料与配方 / 销售与市场 / 会话记录 / 思考收件箱 / 精细化工与扩展 / 工作日志；放不下时新增分区并同步 registry.json；**日化知识之外的内容（AI 经验/日常/收件箱）独立大类，不混入知识分类**
2. **双格式（分级）**：用户看的页 HTML 展示 + MD 供 AI 检索（知识内容/会话经验/工作日志/机制更新/任务板，双格式成对）；**AI 看的页**（ai-worklog、CHANGELOG 等治理记录）可只写 MD，成对校验仅告警不阻断（见铁律 1）
3. **流程**：写内容 → HTML → MD → registry.json → 重新生成 mobile.html → 部署
4. **UI**：卡片式避免文字墙；每页有 hero、分区、数据表（关键数据标来源）、相关页面链接；页面底部不得出现 footer 文案
5. **数据**：具体数据标注来源（产品手册/培训资料）；无法核实的不冒充权威
6. **导航**：顶部导航由 `_shared/js/nav.js` 统一渲染，新增页面只改 nav.js 链接清单；首页不留冗余快捷栏
7. **去向**：闲聊/零散半成品先放"思考收件箱"（按日期/话题分文件），整理成可复用经验后移入"会话记录"

## 三、部署规范

1. **通道**：GitHub API（api.github.com）Contents 上传 + `POST /pages/builds`；git push 仅备选（国内不稳）
2. **流程**：上传 → 触发构建 → 轮询 `status=built` → 通知用户验证
3. **验证**：以服务器端抓取为准（不能只看构建状态）；每次必须 `--expect` 验证本次修改内容
4. **缓存**：改版加版本参数（?v=）；仍不见效让用户清缓存/无痕
5. **敏感信息**：Token 存本地 .env（gitignore），不硬编码、不在对话传递
6. **一键部署**：统一 `node tools/deploy.js`（自动完成：成对校验 → 生成 mobile.html → 串行上传 → 构建 → 验证）；上传必须串行（并发写触发 409）
7. **部署权限范围（2026-08-03 用户拍板"只有专业报告可以部署上线，其他没权限"）**：**主 AI 及各主窗口 AI（闲聊/智能体/日志/UI）本身有部署权利**——正式内容创建/修改后即部署，不受本限制；**大脑体系子 AI（Claude Code 主大脑/技术验证员窗口）仅限专业报告可部署上线**（如技术验证报告），其他产出（测试/临时/未整理内容）无部署权限，需上线先报主 AI 审批

## 四、手机版（mobile.html）

- 内容变更后必须重新生成 mobile.html（deploy.js 已绑定）；生成逻辑与已知坑见 CODE_WIKI §4.1

## 五、会话协作

1. 每次聊天结束/换话题时**自动提炼**（2026-08-03 用户拍板）：把本次内容（聊了什么/结论/用户拍板）记录、总结、精炼 → 会话记录（HTML+MD）；默认追加不新建页（见铁律 5）；会话档案区边界已拍板：**给谁看 = 全员可查、结构 = 并入会话记录**（① ② 已定，换话题自动提炼 = ③ 频率）
2. 每次产出**正式内容**后**立即部署**（deploy.js）；**大改动规则不豁免部署**——正式内容创建/修改后即部署，部署是流程终点，不是架构决定；大脑体系子 AI 的部署权限限定见三-7

## 六、主 agent 规则

1. **职责**：底层架构——registry 索引、mobile 生成逻辑、tools/ 脚本、部署机制、跨会话协调
2. **专属改动（大改动）**：新增/删除/重命名分区、首页导航、registry 结构、style.css、mobile 生成、tools/ 脚本、跨页批量、部署通道
3. **部署职责**：所有部署走 deploy.js；小改动各 agent 自理（主 agent 备案）；大改动主 agent 执行；sha 冲突主 agent 协调
4. **纪律**：改文件前先读最新版；协调分工避免文件冲突；Token 走环境变量
5. **并行分派**：大任务按文件拆子代理并行（各碰各的文件）；关键产出配审核 agent 独立复核后才部署；**同一文件编辑必须串行**
6. **唤醒识别**：见铁律 8
7. **机制变更先审批**：新建/修改 tools/ 脚本、新增机制、工具类变更 → 先报主 AI 审批，获批再执行（不自我授权）

## 七、DoD（任务完成定义）——未达标不算完成

- **通用**：CHANGELOG 已记录 ✓ 线上内容已部署 ✓ `--expect` 验证本次修改上线 ✓
- **记忆类**：干完活/拍板后关键结论已 mem_add 写入记忆库 ✓（防失忆铁律 10）；**状态保鲜**：改过 KEY_MEMORY/交接/机制页 → 已重灌库同步（`kb_seed_rules.py --key-memory` 或 `kb_seed_knowledge.py --domain`）✓
- **内容类**：HTML/MD 成对（AI 看页豁免，见铁律 1）/ registry 登记 / 导航入口 / mobile 重新生成 / --expect / 满足页面写作标准
- **部署类**：只走 deploy.js / built / 通用验证（footer 0 次、面板数） / --expect / 通过才通知
- **铁律**：回复"完成"前必须输出自检清单逐项打勾；任何一项未过 → 继续修复不宣布；用户指出"没做到"= DoD 失效 → 立即补机制
- **用户检查手段**：回复里没有"完成自检清单"逐项打勾 = 未完成，用户可直接判定并指出；AI 立即补上，再重新宣布"完成"

## 八、响应规范（减少用户提示）

**响应四问**（每次回复前必答）：
1. 理解了吗——能一句话复述用户要什么？模糊/讨论型先复述请确认
2. 讨论还是执行——用户说"讨论/完善/你觉得呢"= 只讨论不执行，决定权留给用户；不推销方案、不催执行
3. 读了吗——涉及内容先读完消化（核心观点/发现的问题/要用户定什么）再开口
4. 谁拍板——方案选择权在用户；只提供分析和材料

**行为**：先复述后行动；讨论不抢跑；少用选项框（AskUserQuestion 仅用于明确决策点）；主动查证；知识问题先翻库——回答知识类问题前先查知识库再答，不瞎编

**翻库规则**（完整可执行版见 `.trae/skills/fanku/SKILL.md`，本库所有 AI 适用）：
- **口诀**：先翻库、再开口；带出处、不编造；翻不到、说没有
- **触发**：知识类问题（产品/机理/配方/数据/市场/客户技术疑问）必翻；闲聊、讨论模式不翻
- **检索**：registry.json 定位 → grep 关键词（3-8 个）→ 局部读取（limit ≤500 行，禁止整页通读）→ 多轮迭代（≤5 轮）→ 翻不到明说"库里没有"，用户需要时联网补答（firecrawl，标来源、不入库）
- **回答**：精简 + 带出处"依据：知识库·XX 页"；忠实度（只许有据、不编造）；数据四档（已核实 > 有来源 > 待核实照讲+标注 > 裸数据禁当依据）；销售/场景类问题补"可以这样跟客户讲"
- **反馈**：翻库发现错/缺/无来源 → 记思考收件箱（页面+问题）
- **用户抽查三问**：答所问了吗 / 依据翻对了吗 / 有没有超依据编造

## 九、主动复盘与自我改进（★★ 用户不满必触发）

- **触发**：用户不满/指出问题；同一问题反复 2 次以上；走弯路/返工/误报；新流程首次跑通
- **复盘四问**：哪里走了弯路？根因？用户指出了什么、我本该主动发现什么？能固化为什么规则/工具/检查项？
- **纪律**：主动提交规范修订（写明触发原因+修改内容），不等用户开口；产出同步：CHANGELOG（事实）+ 经验页（可复用经验）+ 规范（规则）
- **留痕分工（2026-08-03 机制瘦身，用户拍板）**：一件事**只记主处，不三处叠罗汉**——CHANGELOG = 唯一全量修改追溯（AI 看）；ai-worklog = 人员一句话（AI 看，细节引用 CHANGELOG）；mechanism-updates = 新机制广播（用户看，保持双格式，稳定即清理）；team-task-board = 任务待办（用户看，保持双格式）；记忆大脑 mem0 = 经验/结论沉淀（与 ai-worklog 分工：它记"谁干了"，mem0 记"结论是什么"，不重复写）
- 历史复盘案例见 CHANGELOG，不在本文档留存叙事

## 十、记忆入库与防失忆（记忆大脑 mem0 + KEY_MEMORY）

- **原理**：对话压缩丢的是"对话流瞬时记忆"（口头承诺/进行中状态/最新决策），文件/库不丢。关键记忆必须落盘，不靠脑子记——承诺只在对话里 = 压缩即蒸发 = 没承诺
- **记忆大脑（mem0，主大脑已建成，M3 达成）**：`D:\ai\brain-memory`，本地免 LLM 语义检索库（fastembed 中文 + qdrant）。分层：短期（run_id 会话内）→ 中期（mid 经验）→ 长期（long 事实）
  - **写**：干完活/拍板后 `mem_add.py --text "<一句结论>" --agent <谁> --layer mid|long`；纠错用 `--fix-target`（删旧写新，真"不再犯"）
  - **读**：开工/压缩后 `mem_search.py --query "<任务关键词>" --limit 3`——从库里想起跨会话经验，不靠脑内残留
  - 完整用法见 `D:\ai\brain-memory\README.md`，命令速查见 KEY_MEMORY⑤
- **记忆分区原则（2026-08-03 用户原话固化）**：**存储端分区、查询端自由**——`agent_id`（谁写的）+ `layer` 打标是"好找"不是"限制"：默认全库语义查（跨 AI 互通，查专业/查感受/查别人日志都行）；需要精准时带 `--agent`/`--layer` 过滤；不设权限墙，该交互就交互
- **自动沉淀 = 默认动作（2026-08-03 用户拍板 B 方案，广播所有 AI）**：话题聊完/干完活，有**结论、决定、教训、偏好**就 mem_add 自动存，不等用户提醒（攒 1-2 个话题存一条，不碎碎念）；**用户拍板的决策/结论 = 最高优先级必存**（闲聊 AI 漏存被用户抓出"这一条你没存？"的教训）；落地要低——先跑通"自动存"最小一步，不搞大蓝图
- **对话存档 = 每轮结束必做（2026-08-03 用户拍板核心）**：AI 不会自动存自己的对话内容是老问题——每轮对话结束（尤其用户拍板/有结论/有教训时），用 `mem_chat_save.py --text "<本轮对话内容/关键结论>" --agent <谁>` 把本轮对话存档（自动切段存 mid 层，source=chat-<agent>-<日期>）；**提炼（把散对话整理成精华）是后面的阶段，先解决"会不会存"**——先存下来，提炼时才有素材
- **速查表**：KEY_MEMORY.md = 压缩/新会话后第一读（身份/铁律/命令/决策一页纸）；只收"丢了会立刻犯错/跑偏/惹烦"的信息，不复制规范全文
- **分层阅读 4 层开工（2026-08-03 用户拍板，库优先/文件兜底，取代原"压缩后恢复三步"）**：
  1. **层1 记忆库 mem0**：知识/规则/经验/决策——默认先查（mem_search，带出处），查不到再翻文件
  2. **层2 KEY_MEMORY**：命令/决策速查——保确定性（不靠检索）
  3. **层3 交接+任务板**：身份/当前状态——migration-handover（+ 角色专属交接）+ team-task-board
  4. **层4 CHANGELOG**：修改追溯/最近进展——看最新条目
- **状态保鲜（2026-08-03 用户拍板）**：KEY_MEMORY/交接改动后重灌库同步（`kb_seed_rules.py --key-memory`，删旧写新）；**所有知识域页**（会话记录/思考收件箱/工作日志等，不只机制页/任务板）改动后跑 `kb_seed_knowledge.py --domain <域> --reset`（幂等，几秒级）；保持"库=文件"。⚠️ mem0 会语义合并旧记忆（删旧可能不彻底、新老版本并存），确定性信息以文件为准，查到矛盾以最新 source/文件为准
- **⚠️ 条数不写死（2026-08-03 用户反馈固化）**：交接/README/机制页**不要写死记忆库/索引条数**（如"~418 条"）——库是持续增长的，写死数字会误导核对者（用户就差点被"~418"误导，实际已是 500+）。需要时说"条数持续增长，以 `mem_search`/`get_collection().points_count` 实时查询为准"
- **强制机制**：
  1. SessionStart 开窗自动读回 KEY_MEMORY 摘要 + mem_search 最近记忆（tools/session_start_report.js）
  2. SessionEnd/PreCompact 自动快照（tools/precompact_save.js，退二线保底）
  3. deploy.js 门禁：部署前强制 CHANGELOG 今日有条目，无则拒绝部署（改必登记变硬约束）
- **压缩方式**：直接打 `/compact` 即可，自动同步记忆
- **改完必回读**：编辑回执"成功" ≠ 磁盘正确（并行编辑同一文件会互相覆盖）；宣布完成前回读实际文件确认
- **fanku 边界（速查表⑥）**：知识类问题（产品/机理/配方/数据/市场/客户技术疑问）必翻；闲聊、讨论模式、文件结构/治理类任务不翻

## 十·一、进化机制（M4 进化大脑，2026-08-03 建）

- **定位**：蓝图第 4 层"进化层"——干完一类活 → 自动提炼经验 → 生成/更新 skill → 越用越准。**M4 第一轮 = 机制 + 工具跑通 + 小规模试点**，不做存量大规模提炼
- **触发条件**（满足其一即触发进化）：
  1. 干完一类活（≥3 个同类任务/页面），有重复可复用的做法
  2. mid 层攒够素材（≥3 条同主题对话存档/经验）
  3. 用户明确要求"做成 skill / 固化 / 以后都这么干"
- **工具**：`node tools/evolve_scan.js --status | --scan [--topic 词] | --draft --id <mid_id> --name <skill名> | --apply --file <草稿> | --rollback --id <id> | --journal`；执行升级走 `D:\ai\brain-memory\scripts\mem_refine.py`（mem0 原生 update 免 LLM，保 id/agent_id/历史）
- **流程**（干完活 → 提炼 → 审批 → 装 skill → 广播）：
  1. `--scan` 看 mid 候选题 → 判断够格（≥3 条同主题 / 含教训·流程·拍板 / 用户要求）
  2. `--draft` 生成 skill 草稿 → 落 `D:\ai\brain-memory\ws\skill-drafts\`（不入库不部署）
  3. **审批（硬门槛）**：草稿登记任务板"待审批 · 认领人:主 AI" → 主 AI 结合 skill-creator 人审（description 触发词/步骤/边界）→ 通过才复制到 `.trae/skills/`；不通过留草稿区打[驳回]标记
  4. `--apply` 把 mid 提炼为 long 经验（source=refined-mid-<日期>，provenance 留痕）
  5. **三处留痕**：ai-worklog 登记 + mechanism-updates 广播 + CHANGELOG 今日条目（deploy 门禁）
- **审批边界**：工具建（evolve_scan.js/mem_refine.py 已授权）/ 草稿生成（可自动）/ **落盘 .trae/skills（主 AI 批，铁律 9）**
- **升级不删原素材**：提炼用 mem0 update 保留 id/agent_id，原文在 mem0_history 可回滚（`--rollback` 恢复原文+layer+source）
- **不做**：不自动静默落盘 skill、不批量升级全部 mid、不接 LLM（保持免 key 纯本地）

### 十·一·A 自动提炼与分类规则（2026-08-03 用户拍板"我来立规则"）

**定位**：把"提炼"从手动升级为**自动提炼 + 自动分类**——你（主大脑）控制脚本，识别该炼什么、自动炼成经验、按受众分类，各 AI 检索时只看到跟自己相关的。

**识别规则（fanku 核对内容，不按 agent 一刀切）**：

| 来源特征 | 判定 | 动作 |
|---|---|---|
| `source` 含 `chat-*`（对话存档） | ✅ 对话 | **提炼**成经验 |
| `source` 含 `*-long`（沉淀经验，如 brain-long/main-long） | ✅ 经验 | 直接作为经验，**不重复炼** |
| `source` = `main-rules` / `KEY_MEMORY` | ❌ 基准 | **不炼**（规则/速查，动了乱套） |
| `source` 含 `xxx.md #`（知识页灌库） | ❌ 知识 | **不炼**（知识库内容，基准） |

**自动提炼流程**（挂心跳，每轮自动跑）：
1. **scan**：扫 mid 候选，按主题聚类（`evolve_scan --scan`）
2. **draft**：自动生成带受众草稿（`--draft --id <mid> --name <skill名> --target <受众>`）
3. **apply**：自动提炼 mid→long（`--apply`，source=refined-mid-<日期>）
4. **分类**：按 `target_agent` 打标，各 AI 检索时 `mem_search --target <受众>` 过滤
5. **留痕**：提炼结果登记 journal + CHANGELOG（不静默）

**自动分类维度**（`--target` 取值）：
- `brain`：主大脑域（调度/机制/接单经验）
- `main`：主 AI 域（统筹/大改动/知识库运营）
- `verifier`：技术验证员域（体检/测试/核对）
- `casual`：闲聊域（用户偏好/印象）
- `agent`：智能体域（翻库/检索/实现）
- `log` / `ui` / `tester`：各对应域
- 缺省 `all`：通用经验，各域都相关

**边界（铁律不豁免）**：
- 提炼**保留原文**（mem0 update 保 id，history 可回滚）
- 提炼**不自动落盘 skill**（skill 落盘仍走审批，铁律 9）；自动提炼只做"mid→long 经验升级 + 分类"
- 提炼**不接 LLM**（免 key 纯本地，mem0 update 免推理）
- 提炼**不批量升级全部 mid**（只炼 `chat-*` 对话 + 够格的经验，规则/知识/基准不动）

**该怎么存（存是炼的前提，2026-08-03 用户拍板"该怎么存"）**：

1. **谁存**：各 AI 干完活/每轮对话结束，自己用 `mem_chat_save.py --agent <自己 id>` 存自己的对话/结论（铁律 9 记忆入库）。Trae 对话由 Trae 侧存，自动接单由 dispatch 存，你（主大脑）干完活也存。
2. **存什么**：本轮**结论 / 用户拍板 / 经验教训 / 干活过程**（有结论、有拍板、有教训必存）；纯闲聊没结论可不存。
3. **存到哪**：`mem_chat_save.py` → mem0 **mid 层**（source=`chat-<agent>-<日期>`），自动切段。**这是提炼的原料层**——炼的就是这里攒的对话。
4. **识别用什么**：存的时候**不用特殊标记**，靠 `source=chat-<agent>-<日期>` 前缀识别（这就是统一格式）。提炼器看 `chat-*` 前缀自动识别，不靠别的。
5. **别漏存**：用户拍板 / 结论 / 教训三类必须存——这是"该存的最小集合"，漏了提炼就缺料。
6. **分清楚**：`chat-*`（对话，待提炼）vs `*-long`（已沉淀经验，不重复炼）vs `main-rules`/`KEY_MEMORY`（基准，不炼）——存的时候各归其位，不混。

**分工（2026-08-03 用户拍板）**：
- **你（Trae/用户）**：负责"存"——让 Trae 对话存进记忆库（你说了算，格式走统一 `mem_chat_save`）
- **我（主大脑）**：负责"提炼 + 分类"——识别该炼什么（`chat-*` 前缀）、自动炼成经验、按 `target_agent` 分类、控制脚本跑

## 十·二、自主运行机制（心跳 + 自动接单，2026-08-03 用户拍板）

- **定位**：把四层大脑从"人工驱动"升级为"自主运行"——用户不用手动敲命令，大脑自己定时维护 + 自动接单处理消息。
- **心跳（brain_heartbeat.js）**：每 20 分钟跑一轮（计划任务 TinciBrainHeartbeat）：
  1. 确保 watch 存活（死了自动拉起）
  2. **M2 索引同步**：跑 update_db.py（必须显式传 `DEEP_MEMORY_WORKSPACE=D:\ai\deep-memory\ws`，否则默认 C 盘搜不到——P0 路径错位根因）
  3. **M3 提炼提醒**：对话存档 chat-* ≥3 提醒（**只提醒不自动提炼**，提炼留人审）
  4. **M5 信箱**：有待处理消息 → spawn brain_dispatch.js 自动接单（每轮上限 2 条）
  - 留痕：只写 watch-log.log（每轮一行 `❤️ 心跳：...`）；**不写 ai-worklog/CHANGELOG、不部署**
- **自动接单（brain_dispatch.js）**：消息投递进信箱 → 秒级（watch 触发）+ 定时（心跳兜底）双路派发 → headless claude 接单处理 → 回写"回复-<原名>.md" → 标 done。
  - **全权接单（用户拍板）**：headless 能读知识库/记忆、改库（HTML+MD 成对）、提炼（evolve --apply）、部署（deploy.js）——但走既有门禁：部署 CHANGELOG 今日条、--expect 验证、双格式成对、DoD 不豁免
  - **安全底线**：token 不直接给 headless（dispatch 层注入）；headless 白名单无 Bash 逃逸路径；预算封顶 $2/单
  - **失败兜底**：失败回滚 status=new + retry；3 次失败标 done 前缀"（需人工）"；不丢单不产生半截回复
- **watch 开机自启**：启动文件夹 brain_watch.vbs（静默拉起 --daemon）；cc-switch 代理已加 Run 自启（headless 依赖）
- **明确不做**：心跳不部署、不自动登 CHANGELOG/ai-worklog；自动接单第一版以"回复+标 done"为基线，改库/提炼/部署逐条加权限实测；不用 MCP/常驻 agent（--bg 编排是终态）
- **投递/回复文件归档（2026-08-03 规则修订固化）**：自动接单中间产物（根目录 `投递-*.md` / `回复-投递-*.md`）处理完（标 done + 用户已确认回复）后，内容已进记忆库（chat-* 存档）+ 提炼为经验，原文件**即删或移入 `.claude/handoffs/archive/`**（监听免疫区，当月清理）；不积压根目录——堆积会干扰"唤醒识别扫 LastWriteTime"（铁律 7）

## 十·三、验证/审核闭环机制（审核→发现→修复→回归，2026-08-03 用户拍板固化）

- **定位**：任何"大改动 / 修 bug / 迁移 / 里程碑达成"宣布完成后，**派子代理独立验证 + 审核**（不能自己验自己）——抓出执行者漏掉的问题，防"修好又被覆盖"、防"登记与实际不符"。
- **触发场景**（有其一即可）：
  1. 修了一个 bug / 迁移了路径 / 重构了工具 —— 宣布"修复完成"前
  2. 里程碑达成（M2/M3/M4/M5）声称"可用" —— 宣布前
  3. 改了多个文件 / 跨页批量 / 动 tools/ —— 部署后
  4. 用户说"复检一下 / 审核一下 / 你确定？" —— 立即
- **两路子代理**（parallel 分派，独立只读）：
  - **验证 agent**：实测跑通、找反例、对照验收标准逐条核对。产出 = 能跑的证据（命令+输出）+ 边界输入反例 + 问题清单。
  - **审核 agent**：工作记录一致性 / 文档数字一致性 / 内容硬伤抽查 / 审批合规抽查。产出 = 对照基准逐条核对 + 问题清单（带路径证据）。
- **闭环五步**：
  1. 主大脑/主 AI 宣布完成 → 登记任务板（认领人=自己）
  2. 派验证 + 审核子代理并行（独立只读，只找问题不修）
  3. 汇总两路意见 → 逐条判定（严重/中等/轻微）→ 修复（严重的必修）
  4. **回归复检**：修复后派子代理复检是否彻底（防"改了可见处漏了隐藏处"——2026-08-03 复检揪出 brain_heartbeat.js 残留 C 盘路径就是典型）
  5. 留痕：修复过程登记 CHANGELOG + 任务板标 ✅；教训沉淀 mem_add
- **核心纪律**：
  - **不能自己验自己**：验证/审核必须独立子代理，禁止执行者自评"没问题"
  - **修复不可只改可见处**：路径/迁移类修复须 grep 全项目所有引用旧路径的活动脚本 + 检查定时任务/守护进程 env（README 改了、heartbeat 漏了 = 修好又被覆盖）
  - **证据优先**：每条意见带路径+内容，不凭印象
  - **只审不改**：验证/审核 agent 只报问题，修复由主大脑/主 AI 做

## 十·四、规则修订体系（2026-08-03 用户拍板"有体系的改"）

> **定位**：规则文档（本规范/机制页/交接/KEY_MEMORY）发现问题 → 有明确闭环处理，不靠人盯、不积压。**所有 AI 适用**——"发现问题只登记，改权在主 AI"。

- **闭环六步**：
  1. **发现**：任何 AI 用规则时发现矛盾/过时/缺失（如引用了不存在的铁律、双格式口径打架）→ 触发登记；用户指出、审核 agent 发现、全局审查（如"现在的文档规则有没有不合理的"）都算
  2. **登记**：发现者一句话记 思考收件箱 `规则问题-<日期>.md`（文件+问题+建议改法），**登记即完成，不自己改**（改权在主 AI）
  3. **评估**：主 AI **积满 3 条或每周**统一处理——判定真伪（误报/真问题）、定改法、列联动面（规范/KEY_MEMORY/机制页/记忆库）
  4. **拍板**：涉及权限/大方向 → 用户拍板；纯文字修正（编号残留/口径统一/引用错误）→ 主 AI 直接改
  5. **修改+同步**：改规范/机制页 → CHANGELOG 登记 → 部署 `--expect` → 状态保鲜重灌记忆库 → 影响其他 AI 行为则 mechanism-updates 广播
  6. **验证**：规则改动属大改动，按十·三派验证 agent 抽查一致性（引用编号/口径/页数/双格式）
- **首批样本（2026-08-03 走通闭环）**：全局审查发现 5 项——①规范残留"铁律 13"引用（实为 0-9）②投递/回复文件无归档规则（根目录积 21 个）③双格式软硬矛盾（铁律 1 告警不阻断 vs DoD 成对硬要求）④状态保鲜范围漏知识内容页 ⑤机制页"稳定即摘除"无责任人。已按本闭环逐项修复 + 广播
- **核心纪律**：只登记不抢改；批量处理不打断日常；改完必走"修改+同步"全套，防止改了文件不同步记忆库（状态保鲜）

## 十一、经验提炼方法论（供"与 AI 的交流经验"页使用）

- 目标：可复用经验，不是流水账
- 句式：`场景/问题 → 做法 → 下次怎么用`
- 只留结论与关键做法；避免时间线流水账、无结论记录、一次性内容

## 十二、工作流程总览（2026-08-03 梳理固化，压缩后按图恢复）

> **一条流水线 + 两套兜底**：开工（4 层恢复）→ 干活（查库优先 + 改动分级）→ 收尾（DoD 门禁）→ 留痕（五处分工）；兜底① = 状态保鲜（改文件必重灌库），兜底② = 防失忆三件套（压缩续命）

### 1. 开工流程（新窗口 / 压缩后，只读 4 处）

| 层 | 读什么 | 目的 |
|---|---|---|
| 层1 | 记忆库 `mem_search` | 知识/规则/经验/决策——默认先查，查不到再翻文件 |
| 层2 | KEY_MEMORY | 命令/决策速查（保确定性，不靠检索） |
| 层3 | 交接 + 任务板 | 身份 / 当前状态（migration-handover + 角色交接 + team-task-board） |
| 层4 | CHANGELOG 最新 | 最近进展（看最新条目） |

### 2. 干活流程

- **响应四问**：理解了吗 / 讨论还是执行 / 读了吗 / 谁拍板
- **查库优先**：知识问题先 `mem_search`（一个口子查所有、带出处），查不到再翻文件
- **改动分级**：小改动各 agent 自理（含部署）；分区/registry/nav/样式/tools/跨页/部署机制 = 大改动，报主 AI
- **防覆盖**：同一文件必须串行（并行会互相覆盖），改完必回读实际文件

### 3. 收尾流程（DoD 门禁，缺一不算完成）

1. **双格式**：用户看页（知识/经验/日志/机制/任务板）HTML+MD 成对；AI 看页（ai-worklog/session-prompt-d）可只写 MD
2. **CHANGELOG 今日登记**（硬门禁：无今日条目 → deploy 拒绝）
3. **部署**：`node tools/deploy.js --files <完整路径> --expect "文件:关键词"`（自动重建 mobile.html，--expect 验证本次修改上线）
4. **记忆沉淀**：结论/拍板 → `mem_add`（long）；每轮对话 → `mem_chat_save`（mid）
5. **留痕登记**：ai-worklog 一句话；新机制 → mechanism-updates 广播；任务 → 任务板更新

### 4. 状态保鲜（改文件必同步库，保持"库=文件"）

| 改动对象 | 动作 |
|---|---|
| KEY_MEMORY / 交接 | `kb_seed_rules.py --key-memory --fix`（删旧写新） |
| 知识页/机制页/任务板 | `kb_seed_knowledge.py --domain <域>`（重灌，几秒级） |

⚠️ mem0 语义合并旧记忆（删旧可能不彻底、新老并存），确定性信息以文件为准，查到矛盾以最新 source 为准。

### 5. 留痕分工（谁记录什么，不叠罗汉）

CHANGELOG = 唯一全量追溯（AI 看）｜ ai-worklog = 人员一句话（AI 看，细节引 CHANGELOG）｜ mechanism-updates = 新机制广播（用户看，稳定即清理）｜ team-task-board = 任务待办（用户看）｜ mem0 = 结论沉淀（AI 查）。

### 6. 防失忆与压缩续命

- **三件套**：SessionStart 开窗读回（自动）→ PreCompact 快照（压缩前落盘 KEY_MEMORY⑨）→ deploy CHANGELOG 门禁（硬约束）
- **压缩**：直接打 `/compact` 即可，自动同步记忆
- **恢复**：走第 1 节 4 层（层1 mem_search → 层2 KEY_MEMORY → 层3 交接+任务板 → 层4 CHANGELOG）
