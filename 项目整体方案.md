# 天赐材料日化知识库 · 项目整体方案

> 版本：v1.0（2026-08-03）· 定位：**项目总纲方案**——背景 → 架构 → 机制 → 流程 → 防失忆 → 大脑蓝图 → 路线图，一页看全。
>
> **文档关系**：规则全集 → 知识库维护规范.md（★★★）｜ 项目背景 → PROJECT_CONTEXT.md（★★）｜ 技术细节 → CODE_WIKI.md（★）｜ 关键记忆 → .claude/handoffs/KEY_MEMORY.md（压缩后第一读）｜ 修改追溯 → CHANGELOG.md
>
> 本文档回答三问：**这个项目是什么？怎么运转？往哪走？**

---

## 一、项目定位

**一句话**：个人可持续迭代的日化知识库——手机上随时可看（不依赖 WiFi，流量即可访问）、AI 随时可调用（HTML+MD 双格式）、持续增量迭代。

- **使用者**：天赐材料（Tinci）日化销售，从"知道产品叫什么"到"理解产品为什么这样"，最终能向客户讲清技术原理
- **内容范围**：表面活性剂、日化原料与配方、销售与市场、精细化工与扩展 + AI 协作/日志/收件箱等非知识内容
- **核心理念**：内容要"可复用经验"（场景→做法→下次怎么用），不要流水账；用户要**专业**不要"懂你"（2026-08-02 敲定交接方向）

## 二、核心架构（双格式 + 静态托管）

| 层 | 组件 | 说明 |
|---|---|---|
| 内容存储 | `domains/<域>/*.html` | 网页展示（卡片式 UI，避免文字墙） |
| 内容存储 | `markdown/<域>/*.md` | AI 检索（带元信息 title/domain/tags/updated） |
| 手机版 | `mobile.html` | 单文件，`tools/build_mobile.js` 动态扫目录生成，**勿手改** |
| 索引 | `registry.json` | 7 域 + 页面元数据注册表 |
| 导航 | `_shared/js/nav.js` | 共享导航，新增页面只改 items 清单 |
| 样式 | `_shared/css/style.css` | 青绿系共享样式 |
| 托管 | GitHub Pages | https://shenlang1111.github.io/tinci-knowledge-base/ |
| 部署 | `tools/deploy.js` | 一键部署（校验→门禁→生成→上传→构建→验证） |

**双格式流程**：写内容 → HTML → MD → registry.json → 重新生成 mobile.html → 部署。任何文件创建/修改后立即部署，不豁免。

## 三、内容体系（7 域 34 页）

| 域 | 页面数 | 内容 |
|---|---|---|
| 表面活性剂 | 11 | 基础概念/四大类/物化性质/合成/应用/产品对照/诊断/新型表活 |
| 日化原料与配方 | 1 | 配方设计（洁面膏/婴童） |
| 销售与市场 | 6 | 市场趋势/话术 + 天赐产品手册（总览/详解/市场话术/两性目录） |
| 精细化工与扩展 | 3 | 助剂/日用化学品/涂料胶粘剂 |
| 思考收件箱 | 7 | 想法缓冲区/智能体存档/任务板/健康度/机制更新/技术验证/AI 工作记录 |
| 会话记录 | 3 | 与 AI 的交流经验/开场话术/用户画像 |
| 工作日志 | 3 | 日志说明 + 按月日志（聚合半月报/月报/年报） |

**内容去向规则**：闲聊/半成品 → 思考收件箱；整理成可复用经验 → 会话记录；默认追加已有页，新建页有门槛且先问用户。
**数据纪律**：具体数据标来源；无法核实标"待核实"不冒充权威；四档分级（已核实 > 有来源 > 待核实照讲+标注 > 裸数据禁当依据）。

## 四、AI 协作机制

**团队**：主 AI（统筹/架构/审批）+ 闲聊 AI（交流）+ 智能体 AI（执行层自动化）+ 日志 AI（日志报告）+ UI 美化 AI + 主大脑/技术验证员（Claude Code 大脑体系）+ 子代理（审核/内容/检索）。

**10 条铁律**（详见规范）：
0 收尾门禁（DoD 清单）· 1 双格式成对 · 2 改动分级（大改动仅主 AI）· 3 部署走 deploy.js+--expect · 4 DoD 逐项打勾 · 5 内容去向 · 6 响应四问 · 7 并行分派（同一文件串行）· 8 唤醒识别 · 9 机制变更先审批

**协作留痕**：任务板（team-task-board 登记/认领/流转）· ai-worklog（谁干了什么）· mechanism-updates（新机制广播，新 AI 必读）· kb-health-checklist（定期体检）· CHANGELOG（修改追溯）。
**翻库纪律**（fanku）：知识问题先翻库再答；带出处、不编造、翻不到明说；闲聊/治理类不翻。

## 五、部署与验证流程

```
node tools/deploy.js --files <清单> --expect "文件:关键词"
  ① 双格式成对校验（警告不阻断）
  ② CHANGELOG 门禁（强制：今日无登记条目 → 拒绝部署）
  ③ 重新生成 mobile.html（绑定）
  ④ 串行上传（sha 冲突检测，并发写触发 409）
  ⑤ 触发 GitHub Pages 构建 + 轮询 built
  ⑥ 服务器端验证（通用检查 + --expect 验证本次修改上线）
```

要点：Token 存 .env（gitignore）；上传必须串行；验证用 Node fetch（PowerShell Invoke-WebRequest 对 GitHub Pages 误报 404）；改版加 ?v= 版本参数绕过缓存。

## 六、防压缩失忆三层强制（2026-08-03 用户拍板）

**问题**：对话压缩会丢失"对话流记忆"（口头承诺/最新状态/半成品），规则和工具因已落文件不丢。真正的失忆源 = **只存在于对话里的信息**。

**方案**（机器强制，不靠自觉）：
| 层 | 机制 | 文件 |
|---|---|---|
| 速查表 | 压缩后第一读：铁律/命令/fanku 边界/工具位置/最近决策/快照区 | `.claude/handoffs/KEY_MEMORY.md` |
| ① 压缩前落盘 | `PreCompact` hook 压缩前自动把任务板/CHANGELOG 状态写入快照区 | `tools/precompact_save.js` |
| ② 开窗读回 | `SessionStart` hook 报告强制输出速查表摘要（含 compact 恢复场景） | `tools/session_start_report.js` |
| ③ 部署门禁 | deploy 前强制 CHANGELOG 今日有条目，否则拒绝部署 | `tools/deploy.js` |

**状态**：官方文档（code.claude.com/docs/en/hooks）确认 PreCompact/PostCompact 为官方事件；脚本本地实测通过；CHANGELOG 门禁实跑通过。唯一待实测：Trae 环境 PreCompact 真实触发（等下一次压缩验证）。
**配套纪律**（AGENT_NOTES）：承诺必落文件（口头承诺=未发生）；改完必回读（编辑回执成功 ≠ 磁盘正确）。

## 七、AI 大脑蓝图（演进路线）

**四层架构**：接入（移动端/多窗口）→ 检索（deep-memory）→ 记忆（记忆层）→ 进化（自我改进）。

| 里程碑 | 内容 | 状态 |
|---|---|---|
| M1 接入 | 移动端写本地文件验证通过（fs.watch 前提成立） | ✅ |
| M2 检索 | deep-memory 正式接入 D:\ai\deep-memory，4 组真实问句全命中，kb_reader 切段 bug 已修复（索引 286→488） | ✅ |
| M3 记忆 | 记忆大脑（mem0 / AI 记忆层，备选方案已定） | ⏳ 待启动 |
| M4 进化 | 进化大脑（自我改进闭环） | ⏳ 待启动 |
| M5 闭环 | 大脑闭环（fs.watch 实时监听，须审批） | ⏳ 待启动 |

**窗口分工**：主大脑（Claude Code）统筹执行 + 技术验证员（试跑/独立复核）+ 主 AI（Trae 前台拍板/写作）。技术选型主选 deep-memory、备选 mem0；仅专业报告可部署上线（用户敲定）。

## 八、关键文件地图

| 文件 | 作用 | 什么时候用 |
|---|---|---|
| 知识库维护规范.md | 总规则 v3.3（10 铁律+分节） | 动手前/被指出问题后必读 |
| PROJECT_CONTEXT.md | 项目背景/用户画像/当前状态 | 新会话接手 |
| CODE_WIKI.md | 技术实现细节/命令 v3.1 | 改脚本/查部署细节 |
| KEY_MEMORY.md | 关键记忆速查表+压缩前快照 | 压缩/新会话后第一读 |
| CHANGELOG.md | 修改追溯（deploy 门禁依赖） | 每次改动后登记 |
| registry.json / nav.js | 页面索引 / 导航清单 | 新增页面时 |
| tools/deploy.js | 一键部署 | 每次内容变更后 |
| tools/build_mobile.js | 生成 mobile.html | deploy 自动绑定 |
| tools/precompact_save.js | 压缩前快照落盘 | PreCompact hook 自动 |
| tools/session_start_report.js | 开窗自动报告 | SessionStart hook 自动 |
| .trae/skills/fanku/ | 翻库检索纪律 | 知识类问题必调 |

## 九、当前状态与下一步

- **已完成**：知识库 7 域 34 页全量上线；双格式/部署/导航/手机版机制健全；检索大脑 M2 达成；防失忆三层强制落地
- **进行中**：待核实数值盘点（60+ 处，等产品手册/TDS 到位后补来源或降级）
- **下一步**：M3 记忆大脑 → M4 进化大脑 → M5 大脑闭环（均须用户审批）；CHANGELOG 本月超 30 条按规则拆分归档

---

> 方案版本 v1.0 · 最后更新 2026-08-03 · 与《知识库维护规范》v3.3 / PROJECT_CONTEXT v3.3 对齐
